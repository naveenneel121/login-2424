apiVersion: v1
kind: ReplicaSet
metadata:
  name: nginx

spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    
    spec:
      containets:
      - name: nginx
        image: nginx
        ports:
        - containerPorts: 80